[project]
name = "domain-experts-tools"
version = "0.1.0-dev"
description = "Companion automation scripts for the domain-experts CC plugin (eval runner, future tooling)."
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.1.71",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
